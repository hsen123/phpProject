const H = require('./common/helper');

$(document).ready(() => {
    H.sortDropdown('#dropdown-wrapper-answer-24 > ul');
    H.sortDropdown('#dropdown-wrapper-answer-25 > ul');
    H.sortDropdown('#dropdown-wrapper-answer-26 > ul');
});
